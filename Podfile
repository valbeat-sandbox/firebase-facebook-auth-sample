# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'stamp-chat' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for stamp-chat
  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  pod 'FBSDKLoginKit'
  target 'stamp-chatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'stamp-chatUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
